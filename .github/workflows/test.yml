name: Test Project
on: push
jobs:
    test:
      runs-on: windows-latest
      steps:
        - name: Get Code
          uses: actions/checkout@v5
        - name: Install Dependencies
          run: npm ci
        - name: Run Tests
          run: npm test
